#include "main.h"

/**
 * main - Prints the alphabet, in lowercase, followed by a new line.
 * Return: Always(0) Success
 */

int main(void)
{
	char = 'a';

	while (char < 'z')
	{
		putchar(char);
		char ++;
	}
	return (0);
}
